---

galaxy_info:
  role_name: sudo
  author: Chris 'sh0shin' Frage
  description: SUDO management
  #company:
  #issue_tracker_url: http://example.com/issue/tracker
  license: BSD-3-Clause

  min_ansible_version: 2.8
  #min_ansible_container_version:

  # https://galaxy.ansible.com/api/v1/platforms/
  platforms:
    - name: Debian
      versions:
        - all

    - name: EL
      versions:
        - all

    - name: FreeBSD
      versions:
        - all

    - name: Ubuntu
      versions:
        - all

  galaxy_tags:
    - sudo
    - sudoers

dependencies: []

# vim: set ft=yaml.ansible sw=2 ts=2 et colorcolumn=160 :
# eof
